<UserControl x:Class="DJBookingSystem.Views.BookingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1160">
    <UserControl.Background>
        <ImageBrush ImageSource="/New-BG.png" Stretch="UniformToFill" Opacity="0.8"/>
    </UserControl.Background>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Loading Overlay -->
        <Border x:Name="LoadingOverlay"
               Grid.RowSpan="3"
               Background="#DD000000"
               Visibility="Collapsed"
               Panel.ZIndex="1000">
            <StackPanel HorizontalAlignment="Center" 
                       VerticalAlignment="Center">
                <TextBlock Text="? LOADING BOOKINGS..."
                          FontSize="24"
                          FontWeight="Bold"
                          Foreground="#00FF00"
                          FontFamily="Consolas"
                          HorizontalAlignment="Center"
                          Margin="0,0,0,20">
                    <TextBlock.Effect>
                        <DropShadowEffect Color="#00FF00" BlurRadius="15" ShadowDepth="0"/>
                    </TextBlock.Effect>
                </TextBlock>
                <TextBlock x:Name="LoadingMessage"
                          Text="Please wait..."
                          FontSize="12"
                          Foreground="#00BFFF"
                          FontFamily="Consolas"
                          HorizontalAlignment="Center"
                          TextWrapping="Wrap"
                          MaxWidth="400"/>
            </StackPanel>
        </Border>

        <!-- Header -->
        <Border Grid.Row="0" Background="#0A0A0A" BorderBrush="#00FF00" BorderThickness="0,0,0,2" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Action Buttons -->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Button Content="+ New Booking" 
                            x:Name="NewBookingButton"
                            Click="NewBooking_Click"
                            Background="#00FF00"
                            Foreground="#000000"
                            FontFamily="Consolas"
                            FontWeight="Bold"
                            Padding="15,8"
                            Margin="5,0"
                            Cursor="Hand"
                            BorderThickness="0">
                        <Button.Effect>
                            <DropShadowEffect Color="#00FF00" BlurRadius="10" ShadowDepth="0" Opacity="0.8"/>
                        </Button.Effect>
                    </Button>
                    <Button Content="Edit" 
                            x:Name="EditButton"
                            Click="EditBooking_Click"
                            Background="#7D7D7D"
                            Foreground="#000000"
                            FontFamily="Consolas"
                            FontWeight="Bold"
                            Padding="15,8"
                            Margin="5,0"
                            Cursor="Hand"
                            BorderThickness="0"/>
                    <Button Content="Delete" 
                            x:Name="DeleteButton"
                            Click="DeleteBooking_Click"
                            Background="#FF0000"
                            Foreground="#FFFFFF"
                            FontFamily="Consolas"
                            FontWeight="Bold"
                            Padding="15,8"
                            Margin="5,0"
                            Cursor="Hand"
                            BorderThickness="0">
                        <Button.Effect>
                            <DropShadowEffect Color="#FF0000" BlurRadius="10" ShadowDepth="0" Opacity="0.6"/>
                        </Button.Effect>
                    </Button>
                    <Button Content="Refresh" 
                            x:Name="RefreshButton"
                            Click="Refresh_Click"
                            Background="#7D7D7D"
                            Foreground="#000000"
                            FontFamily="Consolas"
                            FontWeight="Bold"
                            Padding="15,8"
                            Margin="5,0"
                            Cursor="Hand"
                            BorderThickness="0"/>
                </StackPanel>

                <!-- Title -->
                <TextBlock Grid.Column="1" 
                          Text="BOOKINGS MANAGEMENT" 
                          FontSize="18" 
                          FontWeight="Bold" 
                          Foreground="#00FF00" 
                          FontFamily="Consolas" 
                          HorizontalAlignment="Center" 
                          VerticalAlignment="Center">
                    <TextBlock.Effect>
                        <DropShadowEffect Color="#00FF00" BlurRadius="10" ShadowDepth="0"/>
                    </TextBlock.Effect>
                </TextBlock>

                <!-- Filter Controls + View Toggle -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <!-- View Toggle Button -->
                    <Button x:Name="ViewToggleButton"
                           Content="?? CALENDAR VIEW" 
                           Click="ToggleView_Click"
                           Background="#FF1493"
                           Foreground="#000000"
                           FontFamily="Consolas"
                           FontWeight="Bold"
                           Padding="15,8"
                           Margin="5,0,15,0"
                           Cursor="Hand"
                           BorderThickness="0">
                        <Button.Effect>
                            <DropShadowEffect Color="#FF1493" BlurRadius="10" ShadowDepth="0" Opacity="0.8"/>
                        </Button.Effect>
                    </Button>
                    
                    <TextBlock Text="Filter:" 
                              Foreground="#00FF00" 
                              FontFamily="Consolas"
                              FontWeight="Bold"
                              VerticalAlignment="Center" 
                              Margin="5,0"/>
                    <ComboBox x:Name="FilterComboBox"
                             Width="150" 
                             Margin="5,0"
                             Background="#000000"
                             Foreground="#00FF00"
                             BorderBrush="#00FF00"
                             BorderThickness="2"
                             FontFamily="Consolas"
                             SelectionChanged="FilterComboBox_SelectionChanged">
                        <ComboBoxItem Content="All Bookings" IsSelected="True"/>
                        <ComboBoxItem Content="Upcoming"/>
                        <ComboBoxItem Content="Today"/>
                        <ComboBoxItem Content="This Week"/>
                        <ComboBoxItem Content="Past"/>
                        <ComboBoxItem Content="My Bookings"/>
                        <ComboBoxItem Content="My Venue"/>
                        <ComboBoxItem Content="Cancelled"/>
                    </ComboBox>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Content Area - List or Calendar View -->
        <Grid Grid.Row="1" x:Name="ContentGrid">
            <!-- List View (DataGrid) -->
            <Border x:Name="ListViewBorder"
                   Background="Black" 
                   BorderBrush="#00FF00" 
                   BorderThickness="2" 
                   Margin="10"
                   Visibility="Visible">
                <DataGrid x:Name="BookingsDataGrid" 
                         Background="#0A0A0A" 
                         Foreground="#00FF00" 
                         BorderBrush="#00FF00"
                         AutoGenerateColumns="False"
                         CanUserAddRows="False"
                         CanUserDeleteRows="False"
                         GridLinesVisibility="All"
                         AlternatingRowBackground="#001100"
                         RowBackground="#000000"
                         FontFamily="Consolas"
                         SelectionChanged="BookingsDataGrid_SelectionChanged">
                    
                    <!-- Column Header Style -->
                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#000000"/>
                            <Setter Property="Foreground" Value="#FF1493"/>
                            <Setter Property="FontFamily" Value="Consolas"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="FontSize" Value="12"/>
                            <Setter Property="Padding" Value="10,8"/>
                            <Setter Property="BorderBrush" Value="#FF1493"/>
                            <Setter Property="BorderThickness" Value="0,0,1,2"/>
                            <Setter Property="HorizontalContentAlignment" Value="Left"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>
                    
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="DJ Name" 
                                           Binding="{Binding DJName}" 
                                           Width="150"
                                           IsReadOnly="True"/>
                        <DataGridTextColumn Header="Venue" 
                                           Binding="{Binding VenueName}" 
                                           Width="200"
                                           IsReadOnly="True"/>
                        <DataGridTextColumn Header="Date" 
                                           Binding="{Binding Date}" 
                                           Width="100"
                                           IsReadOnly="True"/>
                        <DataGridTextColumn Header="Time Slot" 
                                           Binding="{Binding TimeSlot}" 
                                           Width="80"
                                           IsReadOnly="True"/>
                        <DataGridTextColumn Header="Status" 
                                           Binding="{Binding Status}" 
                                           Width="100"
                                           IsReadOnly="True"/>
                        <DataGridTextColumn Header="Streaming Link" 
                                           Binding="{Binding StreamingLink}" 
                                           Width="*"
                                           IsReadOnly="True"/>
                        <DataGridTextColumn Header="Booked" 
                                           Binding="{Binding CreatedAt}" 
                                           Width="150"
                                           IsReadOnly="True"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <!-- Calendar View -->
            <ContentControl x:Name="CalendarViewContainer"
                           Visibility="Collapsed"/>
        </Grid>

        <!-- Status Bar -->
        <Border Grid.Row="2" 
               Background="#0A0A0A" 
               BorderBrush="#00FF00" 
               BorderThickness="0,2,0,0" 
               Padding="10,5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock x:Name="StatusText"
                          Grid.Column="0"
                          Text="Ready"
                          Foreground="#00FF00"
                          FontFamily="Consolas"
                          VerticalAlignment="Center"/>
                
                <TextBlock x:Name="BookingCountText"
                          Grid.Column="1"
                          Text="0 bookings"
                          Foreground="#00FF00"
                          FontFamily="Consolas"
                          FontWeight="Bold"
                          VerticalAlignment="Center"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>