<Window x:Class="DJBookingSystem.InteractiveGuide.GuidePlayerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CandyBot Interactive Guide" Height="400" Width="600"
        Background="#1A1A2E" WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize">
    
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#FF1493"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FF69B4"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- CandyBot Logo / Visualizer Area -->
        <Border Grid.Row="0" Background="#2A2A3E" Margin="10" CornerRadius="10">
            <Image Source="/Assets/candybot_rainbow.png" 
                   Stretch="Uniform" 
                   MaxHeight="200"
                   Margin="20"/>
        </Border>
        
        <!-- Progress Bar -->
        <Grid Grid.Row="1" Margin="10,0,10,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock x:Name="CurrentTimeText" 
                       Grid.Column="0"
                       Text="00:00" 
                       Foreground="White" 
                       VerticalAlignment="Center"
                       Margin="0,0,10,0"/>
            
            <Slider x:Name="ProgressSlider" 
                    Grid.Column="1"
                    Minimum="0" 
                    Maximum="100"
                    VerticalAlignment="Center"
                    ValueChanged="ProgressSlider_ValueChanged"
                    Foreground="#FF1493"
                    Background="#444"/>
            
            <TextBlock x:Name="TotalTimeText" 
                       Grid.Column="2"
                       Text="00:00" 
                       Foreground="White" 
                       VerticalAlignment="Center"
                       Margin="10,0,0,0"/>
        </Grid>
        
        <!-- Controls -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" 
                    HorizontalAlignment="Center" Margin="10">
            
            <Button x:Name="PlayButton" Content="▶ Play" Click="PlayButton_Click"/>
            <Button x:Name="StopButton" Content="⏹ Stop" Click="StopButton_Click"/>
            <Button x:Name="RestartButton" Content="⟲ Restart" Click="RestartButton_Click"/>
            <Button x:Name="CloseButton" Content="✕ Close" Click="CloseButton_Click"/>
            
        </StackPanel>
        
    </Grid>
</Window>
