# MainWindow.xaml.cs - Migration TODO List

## Methods That Need Content Migration

The following methods reference controls that no longer exist because we removed the tab-based interface. These methods will work once we move the controls to the appropriate panels.

### 1. **InitializeTimeControls()** - Lines ~774-782
**References:** `HourComboBox`, `MinuteComboBox`  
**Action:** These controls need to be added to `BookingsPanel` when we migrate the booking wizard content.

### 2. **AutoFillDJInfo()** - Lines ~803-820
**References:** `DJNameTextBox`, `StreamingLinkTextBox`  
**Action:** These controls need to be added to `BookingsPanel` when we migrate the booking form.

### 3. **DJNameTextBox_TextChanged()** - Line ~828
**Action:** Will work once DJNameTextBox is in BookingsPanel

### 4. **AddBooking_Click()** - Lines ~777-960
**References:** Multiple booking form controls  
**Action:** Will work once booking form is migrated to BookingsPanel

### 5. **RefreshBookings_Click()** - Lines ~963-980
**References:** `VenueFilterComboBox`, `BookingsDataGrid`  
**Action:** Will work once these are migrated to BookingsPanel

### 6. **VenueFilter_Changed()** - Lines ~1005-1034
**References:** `VenueFilterComboBox`, `BookingsDataGrid`  
**Action:** Will work once these are migrated to BookingsPanel

### 7. **ViewDetails_Click()** - Lines ~1036-1061
**References:** `BookingsDataGrid`  
**Action:** Will work once BookingsDataGrid is in BookingsPanel

### 8. **EditBooking_Click()** - Lines ~1082-1109
**References:** `BookingsDataGrid`  
**Action:** Will work once BookingsDataGrid is in BookingsPanel

### 9. **DeleteBooking_Click()** - Lines ~1109-1143
**References:** `BookingsDataGrid`  
**Action:** Will work once BookingsDataGrid is in BookingsPanel

### 10. **LoadVenuesAsync()** - Lines ~1144-1155
**References:** `VenuesDataGrid`  
**Action:** Will work once VenuesDataGrid is in VenuesPanel

### 11. **PopulateVenueComboBoxes()** - Lines ~1157-1173
**References:** `VenueComboBox`, `VenueFilterComboBox`  
**Action:** Will work once these are migrated

### 12. **RegisterVenue_Click()** - Opens VenueRegistrationWindow
**Status:** ? Already works - opens separate window

### 13. **VenueComboBox_SelectionChanged()** - Check venue availability
**Action:** Will work once control is migrated

### 14. **BookingsDataGrid_SelectionChanged()** - Enable/disable buttons
**Action:** Will work once DataGrid is migrated

### 15. **CopyStreamingLink_Click()** - Copy link from booking
**Action:** Will work once button is in BookingsPanel

### 16. **VenueListingsTab_Loaded()** - Load venue listings
**Action:** Convert to LoadVenueListings() method for VenuesPanel

## Temporary Solution

For now, to make the app build and run, I'll:
1. ? Comment out the broken control references
2. ? Keep the business logic intact
3. ? Add TODO comments for where controls need to be added
4. Move content to panels in next phase

## Build Strategy

### Phase 1: Get It Building ?
- Comment out broken references
- Keep method signatures
- Add null checks

### Phase 2: Move Content to Panels (Next)
- Copy View Bookings tab content ? BookingsPanel
- Copy Open Decks tab content ? VenuesPanel  
- Copy Radio Player tab content ? RadioPanel
- Create new Chat, Theme, Admin, Settings, Help panels

### Phase 3: Reconnect Methods
- Uncomment control references
- Test each panel
- Verify all functionality works

## Files Status

? **MainWindow.xaml** - Redesigned with menu-driven interface  
? **MainWindow.MenuHandlers.cs** - Menu click handlers added  
? **MainWindow.xaml.cs** - Needs control reference cleanup (this file)  
? **Content Migration** - Needs to move tab content to panels  
