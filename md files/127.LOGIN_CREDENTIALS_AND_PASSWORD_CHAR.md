# ?? **LOGIN CREDENTIALS & PASSWORD CHARACTER UPDATE**

---

## ?? **SYSADMIN CREDENTIALS:**

### Username:
```
SysAdmin
```

### Password:
```
Fraser1960@
```

### Full Details:
```
Username: SysAdmin
Password: Fraser1960@
Full Name: System Administrator
Email: sysadmin@djbooking.com
Role: SysAdmin (Full Administrator Access)
```

---

## ?? **PASSWORD CHARACTER CHANGE:**

### Current Password Character:
```xml
PasswordChar="?"
```

### Required Password Character:
```xml
PasswordChar="#"
```

---

## ?? **FILE TO UPDATE:**

**File:** `LoginWindow.xaml`  
**Line:** Approximately 253

### Current Code:
```xml
<PasswordBox x:Name="PasswordBox"
             Height="45"
             Padding="12"
             FontSize="14"
             FontFamily="Consolas"
             Background="#1a1a1a"
             Foreground="#00FF00"
             BorderBrush="#00FF00"
             BorderThickness="2"
             PasswordChar="?"
             PasswordChanged="PasswordBox_PasswordChanged"
             KeyDown="PasswordBox_KeyDown"
             Margin="0,0,0,10">
    <PasswordBox.Effect>
        <DropShadowEffect Color="#00FF00" BlurRadius="5" ShadowDepth="0" Opacity="0.3"/>
    </PasswordBox.Effect>
</PasswordBox>
```

### Updated Code:
```xml
<PasswordBox x:Name="PasswordBox"
             Height="45"
             Padding="12"
             FontSize="14"
             FontFamily="Consolas"
             Background="#1a1a1a"
             Foreground="#00FF00"
             BorderBrush="#00FF00"
             BorderThickness="2"
             PasswordChar="#"
             PasswordChanged="PasswordBox_PasswordChanged"
             KeyDown="PasswordBox_KeyDown"
             Margin="0,0,0,10">
    <PasswordBox.Effect>
        <DropShadowEffect Color="#00FF00" BlurRadius="5" ShadowDepth="0" Opacity="0.3"/>
    </PasswordBox.Effect>
</PasswordBox>
```

---

## ?? **WHERE PASSWORD IS STORED:**

**File:** `Services/CosmosDbService.cs`  
**Method:** `InitializeDefaultAdminAsync()`  
**Lines:** Approximately 138-157

```csharp
public async Task InitializeDefaultAdminAsync()
{
    var users = await GetAllUsersAsync();
    if (!users.Any(u => u.Username.Equals("SysAdmin", StringComparison.OrdinalIgnoreCase)))
    {
        string passwordHash = HashPassword("Fraser1960@");
        await AddUserAsync(new UserModel
        {
            Username = "SysAdmin",
            PasswordHash = passwordHash,
            FullName = "System Administrator",
            Email: "sysadmin@djbooking.com",
            Role = UserRole.SysAdmin,
            Permissions = new UserPermissions(),
            IsActive = true,
            CreatedAt = DateTime.Now,
            IPHistory = new List<string>(),
            AppPreferences = new UserAppPreferences
            {
                ThemeName = "Default",
                AutoLogin = false,
                RememberMe = false
            }
        });
    }
}
```

---

## ? **CHANGES TO MAKE:**

### 1. Update Password Character (LoginWindow.xaml)
Change line 253 from:
```xml
PasswordChar="?"
```
To:
```xml
PasswordChar="#"
```

---

## ?? **RESULT:**

### After Update:
- **Password Display:** When typing password, characters will show as `#` instead of `?`
- **Example:** Typing "Fraser1960@" will display as: `############`

### Login Credentials Remain:
- **Username:** SysAdmin
- **Password:** Fraser1960@
- **Access Level:** Full Administrator

---

## ?? **PASSWORD SECURITY:**

### Password Hash Method:
```csharp
private string HashPassword(string password)
{
    using (var sha256 = System.Security.Cryptography.SHA256.Create())
    {
        byte[] bytes = sha256.ComputeHash(System.Text.Encoding.UTF8.GetBytes(password));
        var builder = new System.Text.StringBuilder();
        foreach (byte b in bytes)
        {
            builder.Append(b.ToString("x2"));
        }
        return builder.ToString();
    }
}
```

**Hash Algorithm:** SHA-256  
**Stored Value:** Hashed version (not plain text)  
**Security:** Password is hashed before storage in database

---

## ?? **QUICK REFERENCE:**

### To Login:
1. Username: `SysAdmin`
2. Password: `Fraser1960@`
3. Password will display as: `############`

### To Change Password Character:
1. Open `LoginWindow.xaml`
2. Find `PasswordChar="?"` (around line 253)
3. Change to `PasswordChar="#"`
4. Save and rebuild

---

## ?? **SECURITY NOTES:**

1. **Password is case-sensitive:** Must be exactly `Fraser1960@`
2. **Password includes special character:** The @ symbol is required
3. **Password is hashed:** Not stored as plain text in database
4. **SysAdmin has full access:** Can manage all users, venues, bookings
5. **Default account:** Created automatically on first run if not exists

---

## ?? **USER ROLES:**

### SysAdmin Permissions:
- ? Full system access
- ? User management
- ? Venue management  
- ? Booking management
- ? Chat moderation
- ? RadioBOSS control
- ? Settings configuration
- ? All features unlocked

---

**Created:** December 2024  
**Status:** Current & Accurate ?  
**Password Character:** To be changed to `#`
