# ?? MainWindow Complete Restructure

## Changes to Implement:

### 1. Fix XAML Structure
- Remove all 11 duplicate venue cards
- Keep only 3 venue cards (Candy Land, Fallen Decks, BatCave)
- Properly close WrapPanel with `</WrapPanel>`
- Close Grid and ScrollViewer properly

### 2. Optimize Window Size
- Change Height from 750 to **700** (smaller, fits better)
- Change Width from 1000 to **950** (slightly smaller)
- MinHeight from 600 to **650** (prevent too small)
- MinWidth from 800 to **900** (prevent too small)

### 3. Remove Scrollbars
- Change all ScrollViewer `VerticalScrollBarVisibility="Auto"` to `"Disabled"`
- Change all ScrollViewer `HorizontalScrollBarVisibility="Auto"` to `"Disabled"`
- Use `Viewbox` for automatic scaling where needed

### 4. Add Zoom Feature
- Add zoom slider in title bar
- Use `ScaleTransform` on main Grid
- Zoom range: 0.7x to 1.5x (70% to 150%)
- Default: 1.0x (100%)
- Keyboard shortcuts: Ctrl+Plus (zoom in), Ctrl+Minus (zoom out), Ctrl+0 (reset)

### 5. RadioBOSS Window Full Screen Support
- Already has ResizeMode="CanResize" ?
- Add WindowState tracking
- When maximized, content scales to fit
- When normal, content fits within window

## Implementation:

Due to file size, I'll create the fix in sections.

**Status**: Ready to implement
