<Window x:Class="DJBookingSystem.FileOrganizerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Candy-Bot File Organizer" Height="600" Width="800"
        Background="#000000" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#002200"/>
            <Setter Property="Foreground" Value="#00FF00"/>
            <Setter Property="BorderBrush" Value="#00FF00"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#00FF00"/>
        </Style>
        
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#00FF00"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <TextBlock Grid.Row="0" Text="🗂️ File Organizer" FontSize="24" FontWeight="Bold" 
                   Margin="0,0,0,20" TextAlignment="Center"
                   Effect="{StaticResource GlowEffect}"/>
        
        <!-- Folder Selection -->
        <StackPanel Grid.Row="1" Margin="0,0,0,20">
            <TextBlock Text="Select Folder to Organize:" FontWeight="Bold" Margin="0,0,0,10"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBox x:Name="FolderPathTextBox" Grid.Column="0" 
                         Background="#001100" Foreground="#00FF00" 
                         BorderBrush="#00FF00" Padding="10" FontSize="14"
                         IsReadOnly="True"/>
                <Button Grid.Column="1" Content="Browse..." Click="BrowseFolder_Click" Width="100"/>
            </Grid>
        </StackPanel>
        
        <!-- Options -->
        <StackPanel Grid.Row="2" Margin="0,0,0,20">
            <TextBlock Text="Options:" FontWeight="Bold" Margin="0,0,0,10"/>
            <CheckBox x:Name="RemoveDuplicatesCheckBox" Content="Remove Duplicate Files" IsChecked="True"/>
            <CheckBox x:Name="OrganizeByDateCheckBox" Content="Organize by Date (YYYY-MM folders)"/>
            <CheckBox x:Name="CleanEmptyFoldersCheckBox" Content="Clean Empty Folders" IsChecked="True"/>
        </StackPanel>
        
        <!-- Results Area -->
        <Border Grid.Row="3" BorderBrush="#00FF00" BorderThickness="1" Padding="10" Margin="0,0,0,20">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="ResultsTextBlock" TextWrapping="Wrap" FontFamily="Consolas"/>
            </ScrollViewer>
        </Border>
        
        <!-- Action Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="📊 Analyze" Click="Analyze_Click" Width="120"/>
            <Button Content="🗂️ Organize Files" Click="Organize_Click" Width="140"/>
            <Button Content="🔍 Find Duplicates" Click="FindDuplicates_Click" Width="150"/>
            <Button Content="❌ Close" Click="Close_Click" Width="100"/>
        </StackPanel>
    </Grid>
</Window>
